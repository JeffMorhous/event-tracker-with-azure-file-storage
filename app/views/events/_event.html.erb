<div id="<%= dom_id event %>">
  <p>
    <strong>Title:</strong>
    <%= event.title %>
  </p>

  <p>
    <strong>Date:</strong>
    <%= event.date %>
  </p>

  <p>
    <strong>Notes:</strong>
    <a href="<%= event.notes.url %>"><%= event.notes.filename %></a>
  </p>

</div>
